interface SujetsData {
    matiere_id: number;
    chapitre_id?: number;
    image: string;
}

interface SujetData {
    image: string,
    author: string,
    chapitre?: string,
    matiere: string
}